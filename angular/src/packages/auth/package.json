{
    "name": "@package/auth",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "ng build @package/auth --watch",
        "build": "ng build @package/auth"
    }
}