{
    "name": "@app/admin",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "ng serve admin --watch",
        "build": "ng build admin --configuration development"
    },
    "dependencies": {
        "@package/auth": "*"
    }
}